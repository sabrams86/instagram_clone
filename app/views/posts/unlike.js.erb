$('#unlike_<%= @post.id %>')
  .removeClass('glyphicon-heart')
  .addClass('glyphicon-heart-empty')
  .attr('id', 'like_<%= @post.id %>')
  .attr('href', '<%= like_post_path(@post.id) %>');
$("#likes_<%= @post.id %>")
  .html("<%= j (render partial: 'posts/likes', locals: { post: @post} ) %>");
