$('#like_<%= @post.id %>')
  .removeClass('glyphicon-heart-empty')
  .addClass('glyphicon-heart')
  .attr('id', 'unlike_<%= @post.id %>')
  .attr('href', '<%= unlike_post_path(@post.id) %>');
$("#likes_<%= @post.id %>").html("<%= j (render partial: 'posts/likes', locals: { post: @post} ) %>");
